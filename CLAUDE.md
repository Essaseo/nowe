# CLAUDE.md

Project guide for AI assistants working on the **nowe** repository.

## Project Overview

**nowe** is a new project in its initial stage. The repository currently contains only a README and this guide. As the project evolves, this file should be updated to reflect the current architecture, conventions, and workflows.

## Repository Structure

```
nowe/
├── README.md        # Project readme
├── CLAUDE.md        # This file — AI assistant guide
└── .git/            # Git repository
```

## Development Setup

No build system, package manager, or dependencies are configured yet. Update this section once the tech stack is chosen.

## Common Commands

_None defined yet._ As scripts and tooling are added, document them here:

```
# Examples (uncomment/replace when applicable):
# npm install          — install dependencies
# npm run dev          — start dev server
# npm run build        — production build
# npm test             — run tests
# npm run lint         — run linter
```

## Code Conventions

No conventions have been established yet. When they are, document:

- Language and framework choices
- Directory structure conventions (e.g., `src/`, `tests/`, `lib/`)
- Naming conventions (files, variables, components)
- Formatting and linting rules
- Import ordering
- Error handling patterns

## Testing

No test framework is configured. Update this section with:

- Test runner and assertion library
- How to run tests
- Test file naming and location conventions
- Coverage requirements

## Git Workflow

- **Default branch:** `main`
- Write clear, concise commit messages focused on the "why"
- Keep commits atomic — one logical change per commit

## Key Decisions Log

Track significant architectural or tooling decisions here as the project develops.

| Date | Decision | Rationale |
|------|----------|-----------|
| — | — | — |
